= Variant aware dependency resolution in Gradle

Describes work in progress in Gradle, for JVM variant aware dependency resolution

Build slides:

```
./gradlew asciidoctor
```

Generate PDF:

```
./gradlew generatePdf
```

